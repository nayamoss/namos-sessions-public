export function speakerTrackingSummary(speakers: Array<{ bio?: string; headshotStorageKey?: string; outstandingTasks: number }>) { return { total: speakers.length, missingBio: speakers.filter(speaker => !speaker.bio).length, missingHeadshot: speakers.filter(speaker => !speaker.headshotStorageKey).length, outstandingTasks: speakers.reduce((total, speaker) => total + speaker.outstandingTasks, 0) }; }
